<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/white">

    <Button
        android:id="@+id/startIsbn"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/none"
        android:paddingTop="4dip"
        android:drawableTop="@drawable/icon_book"
        android:focusable="true"
        android:gravity="center_horizontal"
        android:text="ISBN"
        android:textColor="@color/great_blue"
        android:textSize="14sp"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="119dp" />
    <Button
        android:id="@+id/startTranslation"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:background="@color/none"
        android:paddingTop="4dip"
        android:drawableTop="@drawable/icon_trans"
        android:focusable="true"
        android:gravity="center_horizontal"
        android:text="Translation"
        android:textColor="@color/great_blue"
        android:textSize="14sp"
        android:layout_above="@+id/startIsbn"
        android:layout_toRightOf="@+id/startIsbn"
        android:layout_toEndOf="@+id/startIsbn" />


</RelativeLayout>